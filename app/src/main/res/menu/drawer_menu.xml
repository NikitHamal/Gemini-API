<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group
        android:id="@+id/group_conversations"
        android:checkableBehavior="single">
        <!-- Conversation list will be populated here dynamically -->
    </group>

    <group
        android:id="@+id/group_actions"
        android:checkableBehavior="none">
        <item
            android:id="@+id/nav_new_chat"
            android:icon="@drawable/ic_add_circle_outline"
            android:title="New Chat" />
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_settings"
            android:title="Settings" />
    </group>

</menu>
